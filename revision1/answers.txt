pwd
ls -la
whoami
hostname
date
mkdir evidence logs notes scripts
cd logs
touch access.log auth.log firewall.log
cd ..
cd evi 'tab'
cd ..
touch student.txt
echo $(whoami) > student.txt
cat student.txt
echo $(hostname) >> student.txt | echo $(date) >> student.txt
cat student.txt
man ls
ls -hl
man mkdir
mkdir -p reports/weekly/week01
name="Gracjan"
course="Cyber"
year="2"
echo "$name is studying $course in Year $year"
echo "$USER"
echo "$HOME"
echo "$SHELL"
echo "$PATH"
path
which python3
which bash
which grep
which cat
echo "Module: $module"
echo 'Module: $module'
name=$(whoami)
host=$(hostname)
dir=$(pwd)
echo "User $name is logged into $host and is currently in $dir"
ls -l | wc -l
env | wc -l
env | grep PATH
ls -l /usr/bin | grep python
ls -l /usr/bin | grep ssh | wc -l
python_count=$(ls -l /usr/bin | grep python | wc -l)
echo "Python-related filenames found: $python_count"